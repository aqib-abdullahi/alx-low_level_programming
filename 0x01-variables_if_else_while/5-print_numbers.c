#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: a program that prints all single
 * digit numbers of base 10 starting from 0
 *
 * Return: Alway 0 (Success)
 */

int main(void)
{

	int num;

	for (num =i 0; num < 10; num++)
	{
		printf("%d", num);
		printf("\n");
	}

	return (0);
		

}
