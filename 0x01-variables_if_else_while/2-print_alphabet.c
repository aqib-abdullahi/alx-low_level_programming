#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Demonstrating the putchar function.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char ch;
	char nl;

	nl = "\n"

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch)
		putchar(nl)

	return (0);

}
