#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Printing the numbers of base 16
 * in lower case.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	int i;
	char ch;

	for (i = 0; i < 10; i++)
		putchar(i);

	for (ch = 'a'; ch <= f; ch++)
		putchar(ch);


	putchar('\n');
	return (0);

}
