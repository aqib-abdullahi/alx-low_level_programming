#include "main.h"

/**
 * add - add two integers
 * @x - int type number
 * @y - int type number
 * Description: Adding two integers
 * Return: Addition of integers
 */

int add(int x, int y)
{
	return (x + y);
}
